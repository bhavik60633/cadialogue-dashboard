"""
CADialogue SEO Engine — autonomous AI-powered SEO growth system.

Modules:
  embeddings_store   — OpenAI text-embedding-3-small, cosine similarity, JSON persistence
  internal_linker    — 30+ internal links per article, backlink injection
  keyword_engine     — Keyword discovery, clustering, difficulty scoring
  topic_authority    — Topic cluster mapping, hub/spoke architecture
  seo_scorer         — Per-article SEO quality score (0-100)
  content_freshness  — Decay detection, stale content alerts
  programmatic_seo   — Template-based page generation
  sitemap_manager    — XML sitemap automation + Google/IndexNow pings
"""
